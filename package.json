{
  "name": "logseq-rename-page-shortcut",
  "version": "0.1.1",
  "author": "Fabian Stanke",
  "main": "dist/index.html",
  "description": "Registers a shortcut in Logseq to rename the current page.",
  "license": "MIT",
  "scripts": {
    "build": "esbuild src/index.js --bundle --outfile=dist/index.js --minify",
    "dev": "esbuild src/index.js --bundle --outfile=dist/index.js --sourcemap --watch"
  },
  "devDependencies": {
    "@logseq/libs": "^0.0.1-alpha.31",
    "esbuild": "^0.15.0"
  },
  "dependencies": {},
  "logseq": {
    "id": "logseq-rename-page-shortcut",
    "title": "Rename Page Shortcut",
    "icon": "icon.png"
  }
}
